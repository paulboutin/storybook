image: buildkite/puppeteer:v3.0.4

pipelines:
  default:
    - step:
        caches:
          - node
        script:
          - apt-get update && apt-get -y install procps
          - npm install
          - npm test
        artifacts:
          - test/**/__diff_output__/*.png
